# JSDOM getComputedStyle example

This is to showcase the JSDOM's getComputedStyle module return pseudo classes when it shouldn't

## Instructions

1. Start with `yarn run dev`
2. Test with `yarn run test`
